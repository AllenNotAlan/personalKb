---
description: Automatically track the tech stack and architecture of the current project.
---

1. Run the `Sync-TechStack.ps1` script to detect the foundational tech stack.
// turbo
2. Execute: `analyze-stack`
3. View the generated `knowledge/project_architecture.md`.
4. Analyze the project's file structure and main modules.
5. Update `knowledge/project_architecture.md` with:
    - High-level component description.
    - Key architectural decisions (e.g., "Using Repository pattern for data access").
    - Identified entry points (e.g., `main.py`, `index.js`).
6. Confirm the documentation accurately reflects the project.
